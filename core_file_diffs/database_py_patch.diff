--- database.py	2024-11-06 17:25:10.493281454 +0000
+++ database.py.origi	2024-11-06 17:22:45.473061948 +0000
@@ -141,7 +141,7 @@
             return self._render_template(error=error)
 
     @http.route('/web/database/restore', type='http', auth="none", methods=['POST'], csrf=False)
-    def restore(self, master_pwd, backup_file, name, copy=False, neutralize_database=False, restore_purpose=None, restorer_name=None):
+    def restore(self, master_pwd, backup_file, name, copy=False, neutralize_database=False):
         insecure = odoo.tools.config.verify_admin_password('admin')
         if insecure and master_pwd:
             dispatch_rpc('db', 'change_admin_password', ["admin", master_pwd])
@@ -151,11 +151,6 @@
             with tempfile.NamedTemporaryFile(delete=False) as data_file:
                 backup_file.save(data_file)
             db.restore_db(name, data_file.name, str2bool(copy), neutralize_database)
-
-	    # Log restore details
-            _logger.info(f"Database '{name}' restored by {restorer_name} for purpose: {restore_purpose}")
-
-
             return request.redirect('/web/database/manager')
         except Exception as e:
             error = "Database restore error: %s" % (str(e) or repr(e))
